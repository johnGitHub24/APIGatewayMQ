# APIGatewayMQ — 初學者學習說明書

> 第一次跑起來、第一次下單成功。  
> 建議先完成 [Trading System MVP](../Trading%20System%20MVP) 的初學者說明，理解風控與狀態機後再學本專案。

**互動教學：** 瀏覽器開啟 [`docs/專案引導教學.html`](專案引導教學.html) 可查看 Mermaid 架構圖與名詞解釋。

---

## 0. 30 秒理解本專案

```text
限流入口 → 202 快速回應 → Kafka 削峰 → Engine 風控成交 → 輪詢查結果
```

與 MVP 的最大差異：**下單走 Gateway 回 202（非同步）**，不再等 Engine 跑完才回應。

---

## 1. 環境需求

| 項目 | 版本 | 檢查方式 |
|------|------|----------|
| JDK | 21 | `java -version` |
| Docker Desktop | 最新版 | `docker info` |
| PowerShell | 5.1+ | 內建 |

設定 JAVA_HOME（Windows 範例）：

```powershell
$env:JAVA_HOME = "C:\Program Files\Java\jdk-21"
$env:PATH = "$env:JAVA_HOME\bin;$env:PATH"
java -version
```

或使用專案腳本：

```powershell
. .\scripts\env.ps1
```

---

## 2. 一鍵啟動（Docker 全棧）

```powershell
cd "D:\ClaudeCode\APIGatewayMQ"
.\scripts\start.ps1
```

腳本會執行 `gradlew bootJar` 後 `docker compose up -d --build`，等待 postgres、kafka 等服務 healthy。

### 服務一覽

| 服務 | URL | 說明 |
|------|-----|------|
| Gateway API | http://localhost:8080 | **對外入口**（下單、查詢） |
| Engine-1 | http://localhost:8081 | 交易引擎副本 1 |
| Engine-2 | http://localhost:8082 | 交易引擎副本 2 |
| Prometheus | http://localhost:9090 | 指標收集 |
| Grafana | http://localhost:3000 | 儀表板（admin/admin） |
| Swagger (Gateway) | http://localhost:8080/swagger-ui.html | API 文件 |
| Swagger (Engine) | http://localhost:8081/swagger-ui/index.html | Engine API 文件 |

### 驗證全棧是否正常

```powershell
.\scripts\verify-docker.ps1
```

等同 `start.ps1` + `smoke-test.ps1`（自動下單並輪詢）。

---

## 3. 第一次下單

### Step 1：非同步下單（經 Gateway）

```http
POST http://localhost:8080/api/v1/orders
Content-Type: application/json
Idempotency-Key: my-first-order

{
  "symbol": "BTCUSDT",
  "side": "BUY",
  "quantity": 0.5,
  "price": 65000
}
```

預期回應：**202 Accepted**

```json
{
  "commandId": "550e8400-...",
  "status": "ACCEPTED",
  "clientOrderId": "my-first-order",
  "message": "Order queued for processing",
  "pollUrl": "/api/v1/orders?clientOrderId=my-first-order",
  "acceptedAt": "2026-07-06T13:00:00Z"
}
```

> **重點：** 202 只代表「已排進 Kafka」，不代表訂單已成交。必須用 `pollUrl` 輪詢。

### Step 2：輪詢結果

```http
GET http://localhost:8080/api/v1/orders?clientOrderId=my-first-order
```

等待 1～3 秒後重試，直到看到 `status: "FILLED"` 或 `"REJECTED"`。

```json
{
  "clientOrderId": "my-first-order",
  "symbol": "BTCUSDT",
  "status": "FILLED",
  "quantity": 0.5,
  "filledQuantity": 0.5
}
```

### Step 3：查持倉與損益

```http
GET http://localhost:8080/api/v1/positions
GET http://localhost:8080/api/v1/pnl
```

### PowerShell 一鍵測試（Invoke-RestMethod）

```powershell
$headers = @{ "Idempotency-Key" = "ps-demo-001" }
$body = '{"symbol":"BTCUSDT","side":"BUY","quantity":0.5,"price":65000}'
Invoke-RestMethod -Method POST -Uri "http://localhost:8080/api/v1/orders" `
  -Headers $headers -ContentType "application/json" -Body $body

Start-Sleep -Seconds 2
Invoke-RestMethod -Uri "http://localhost:8080/api/v1/orders?clientOrderId=ps-demo-001"
```

---

## 4. 常見問題

| 症狀 | 可能原因 | 處理方式 |
|------|----------|----------|
| `docker info` 失敗 | Docker Desktop 未啟動 | 開啟 Docker Desktop 後重試 |
| 202 後輪詢一直 404 | Engine 尚未消費完 | 等 2～5 秒再輪詢 |
| 429 Too Many Requests | 超過限流（預設 100 req/s） | 等 1 秒後重試；見 `Retry-After` Header |
| 409 Conflict | 同一 Idempotency-Key 重複下單 | 預期行為（冪等保護） |
| 422 Unprocessable | 風控拒絕 | 檢查 `errorCode`、`ruleCode` |
| 連不上 8080 | Gateway 容器未啟動 | `docker compose ps` 檢查狀態 |

---

## 5. 跑測試（不需 Docker）

```powershell
cd "D:\ClaudeCode\APIGatewayMQ"
. .\scripts\env.ps1
.\gradlew.bat check
```

測試使用 **H2 + EmbeddedKafka**，不需 Docker。等同 CI 執行的驗證。

只跑特定模組：

```powershell
.\gradlew.bat :gateway:test
.\gradlew.bat :engine:integrationTest
```

---

## 6. 壓力測試（可選）

驗證限流與削峰效果：

```powershell
.\scripts\load-test.ps1 -Requests 200 -Concurrency 20
```

觀察 Prometheus `http://localhost:9090` 中的 Gateway metrics。

---

## 7. 停止服務

```powershell
docker compose down
```

清除資料卷（重置 DB）：

```powershell
docker compose down -v
```

---

## 8. 本機開發模式（進階）

只啟動基礎設施，Java 本機跑：

```powershell
docker compose up -d postgres redis zookeeper kafka

# 終端 1
.\gradlew.bat :engine:bootRun --args="--server.port=8081"

# 終端 2
.\gradlew.bat :engine:bootRun --args="--server.port=8082"

# 終端 3
.\gradlew.bat :gateway:bootRun
```

---

## 9. 學習路線

| 順序 | 做什麼 | 讀什麼 |
|------|--------|--------|
| 1 | 跑通本文件 §2～§3 | 本文件 |
| 2 | 看互動架構圖 | `docs/專案引導教學.html` |
| 3 | 理解非同步流程 | `docs/功能流程說明.md` |
| 4 | 查 API 細節 | `API規格書.md` |
| 5 | 深入架構 | `docs/架構學習導引.md` |
| 6 | 面試準備 | `APIGatewayMQ 規格書.md`、`卡牌步驟.md` |

---

*最後更新：2026-07-07*
